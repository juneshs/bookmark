export class Projectbookmark {
  constructor(public id: number,
              public name: string,
              public projectId: Number,
              public type: string,
              public url: string) {
  }
}
